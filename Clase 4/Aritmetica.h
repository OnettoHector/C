#include <stdio.h>

/** \brief pide un numero y lo valida
           en un rango especifico
*
* \param char[] texto que se va a mostrar
* \param int el minimo valor dentro del rango
* \param el maximo valor dentro del rango
* \return int [1] en caso de exito [0] en caso de falla
*/
int pedirNum(char msj [], int numMin, int numMax);
